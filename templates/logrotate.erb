/var/log/supervisor/supervisord.log /var/log/supervisor/*/*.out /var/log/supervisor/*/*.err {
<%= supervisor_log%> <%supervisor_log_dir%>/*/*.out <%supervisor_log_dir%>/*/*.err {	
    rotate 5
    daily
    compress
    postrotate
        /usr/bin/killall -USR2 supervisord
    endscript
}
